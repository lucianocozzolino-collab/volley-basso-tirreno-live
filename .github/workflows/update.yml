name: Aggiorna Volley

on:

  workflow_dispatch:

  schedule:
    - cron: '0 */6 * * *'

permissions:
  contents* write

jobs:

  update:

    runs*on: ubuntu-latest

    steps:

   *  - uses: actions/checkout@v4

*     - uses: actions/setup*node@v4
        with:
          no*e-version: 22

      - run: npm in*tall

      - run: node scripts/up*ate.js

      - name: Commit
     *  run: |

          git config --g*obal user.name "github-actions"
  *       git*config*--global user.email "github-action*@github.com"

          git add .
*          git diff --staged --quie* || git commit -m "Aggiornamento a*tomatico"

          git push
